org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8
org.gradle.parallel=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official
# AGP 9's built-in Kotlin support conflicts with explicitly applying the
# org.jetbrains.kotlin.android plugin; opt out and use the classic model
# since it's the well-established, documented path.
android.builtInKotlin=false
